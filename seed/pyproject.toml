[project]
name = "velora-seed"
version = "0.1.0"
description = "Velora one-shot seed: backfill prices (Binance REST) and tweets (Nitter → Kafka)"
requires-python = ">=3.11"
dependencies = [
    "asyncpg>=0.30.0",
    "kafka-python>=2.0.2",
    "playwright>=1.48.0",
    "pydantic>=2.9.0",
    "pydantic-settings>=2.6.0",
]

[tool.uv]
package = false
