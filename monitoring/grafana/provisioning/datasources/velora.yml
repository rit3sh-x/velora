apiVersion: 1

datasources:
  - name: Velora
    uid: velora
    type: yesoreyeram-infinity-datasource
    access: proxy
    url: http://host.docker.internal:8000
    isDefault: true
    editable: true
    jsonData:
      auth_method: none
      global_queries: []
