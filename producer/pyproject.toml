[project]
name = "velora-producer"
version = "0.1.0"
description = "Velora producer: Binance WS + Nitter Playwright → Kafka"
requires-python = ">=3.11"
dependencies = [
    "kafka-python>=2.0.2",
    "websockets>=12.0",
    "playwright>=1.48.0",
    "apscheduler>=3.10.4",
    "pydantic>=2.9.0",
    "pydantic-settings>=2.6.0",
]

[tool.uv]
package = false
